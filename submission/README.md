
# Submission workspace

官方原始模板位于：

```text
template/original_answer_sheet.htm
```

当前尚未完成最终提交页，因此没有创建容易被误认为正式成品的 `index.htm`。

最终整理时建议：

1. 复制模板为 `index.htm`。
2. 将图片复制到 `assets/images/`。
3. 将交互所需 CSV/JSON 复制到 `assets/data/`。
4. 将 CSS 与 JavaScript 分开放置。
5. 使用相对路径，并在断网环境下测试。
6. 确认每题答案和图片数量符合题目要求。
